syntax = "proto2";
package bk;

message Bookmark {
  optional string description = 2;
  optional string command = 3;
  enum Action {
    OPEN = 0;
    WEB = 1;
    COPY = 2;
  }
  optional Action action = 4 [default = OPEN];
}

message Book {
  repeated Bookmark bookmarks = 1;
}